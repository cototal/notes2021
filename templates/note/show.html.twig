{% extends 'base.html.twig' %}

{% block title %}{{ note.title }}{% endblock %}

{% block body %}
    <div class="row mt-4">
        <div class="col-md-4 mb-4">
            <h1>{{ note.title }}</h1>
        </div>
        {{ content | raw }}
    </div>
{% endblock %}
